# Massively theme source reference

This directory preserves the upstream Sass source for the HTML5 UP Massively theme. It is not compiled by this repository's build; the published theme CSS is `assets/css/main.css`.

Treat these files as vendor reference material. A future Sass workflow should be introduced explicitly rather than assuming edits here affect the deployed site.
